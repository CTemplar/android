<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/item_folder_holder_ico"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:padding="10dp"
        android:src="@drawable/ic_manage_folders" />

    <TextView
        android:id="@+id/item_folder_holder_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:fontFamily="@font/lato_regular"
        android:gravity="right"
        android:lines="1"
        android:maxWidth="140dp"
        android:text="Folder"
        android:textColor="@color/colorBlack4"
        android:textSize="16sp"
        app:layout_constraintLeft_toRightOf="@id/item_folder_holder_ico"
        app:layout_constraintTop_toTopOf="@id/item_folder_holder_ico"
        app:layout_constraintBottom_toBottomOf="@id/item_folder_holder_ico" />

</androidx.constraintlayout.widget.ConstraintLayout>